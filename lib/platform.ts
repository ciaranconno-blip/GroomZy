// The uid that owns groomzy.ie itself (Fairy Dog Mother's account, doubling
// as the platform operator for now — there's only one person running this).
// Gates the /dev/analytics page and its API route so no other groomer can
// see platform-wide numbers.
export const PLATFORM_OWNER_UID = "urJLvy0SAST9Th5CMVOA6HII3n62";
